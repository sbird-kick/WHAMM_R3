whamm: instr stack-overflows (SIGABRT "thread 'main' has overflowed its stack") on SQLite amalgamation module (2332 fns, ~718KB); with 64MB stack it no longer crashes but instr does not finish within 2min. Likely recursive instruction/AST walker blows the stack on SQLite's giant functions (e.g. sqlite3VdbeExec). Repro: build .c per db_BUILD.txt, then `whamm instr` with the r3 .mm. SQLite unshippable as an instrumented diff test until fixed.
