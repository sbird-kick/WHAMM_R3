whamm: THROUGHPUT ONLY (corrected 2026-07-25). The original SIGABRT was the
deep-predicate overflow (pre-fix script_gen emitted a flat 2337-term chain);
with balanced predicates the module instruments without crashing but takes
>10min in debug whamm. A synthetic single-function sweep to 4,000,000 flat
instructions never crashed, ruling out a separate giant-function walker bug.
Runs correctly uninstrumented (count=20 sum=118 max=12). Blocked on whamm
instrumentation speed, nothing else.
